import User, { initializeUser } from "./user.model";
import Restaurant, { initializeRestaurant } from "./resturent.model";
import Category, { initializeCategory } from "./category.model";

// export { User, Restaurant, Category };
export {
  User,
  initializeUser,
  Category,
  initializeCategory,
  Restaurant,
  initializeRestaurant,
};
